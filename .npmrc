# esbuild/lightningcss sont des binaires pré-compilés ; on saute la vérif de deps
# avant l'exécution des scripts (sinon le warning "ignored builds" fait échouer les scripts).
verify-deps-before-run=false
